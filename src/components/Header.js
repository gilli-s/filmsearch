import React from "react";

const Header = (props) =>{
    return(
        <header className = "App-header">
            <h2>Хэдэр моего нового приложения</h2>
        </header>
    );
};

export default Header;
